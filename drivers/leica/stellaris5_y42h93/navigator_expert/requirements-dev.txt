# Test/CI dependencies for the Navigator Expert driver offline suite.
#
# One source of truth for what the suite needs, installed both by CI
# (.github/workflows/navigator-expert.yml) and locally:
#     python -m pip install -r requirements-dev.txt
#     python run_ci.py
#
# NOTE: pythonnet / the LAS X CAM runtime are deliberately NOT here -- they are
# only needed for the live @hardware tests on a microscope PC, not the offline
# suite. The offline suite runs anywhere (Linux/macOS CI included).
pytest>=8
pytest-cov>=5
ruff>=0.6
numpy>=1.24
scipy>=1.10
tifffile>=2024.1
pillow>=10
