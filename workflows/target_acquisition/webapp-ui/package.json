{
  "name": "zmart-webapp-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Front end for the target-acquisition operator page. Built to a single self-contained file that the Python server hands out.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "vite": "^8.1.5",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
