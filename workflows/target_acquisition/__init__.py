"""Target-acquisition workflow: low-magnification overview, cellpose-driven
cell selection, high-magnification re-imaging across an objective switch.

Operator entry is `zmart_microscopy_v4.ipynb`; `_bootstrap.py` sets `sys.path` so the
notebook can import the controller, driver registration, and shared packages.
Implementation lives in `workflow/`.
"""
