"""Calibration workflow modules.

Thin glue between the calibration notebooks and the driver / algorithms
layers. Each module owns one calibration's I/O, schema construction, and
visualization; notebooks call one function per cell.
"""
