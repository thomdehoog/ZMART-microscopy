# Findings Verification — Adversarial Re-check of All Critical/High Findings

- **Date:** 2026-07-03 · **Commit:** `c7964dd` (working tree == origin/main code; only `docs/reviews/*.md` added)
- **Inputs:** the eleven review documents in `docs/reviews/` (`zmart_controller_review`, `leica_commands_connection_readers_review`, `leica_scanfields_acquisition_review`, `leica_calibration_config_motion_review`, `leica_zmart_adapter_toplevel_review`, `leica_tests_ci_review`, `fossils_dead_code_review`, `overfitting_patchwork_review`, `refactor_opportunities_review`, `concurrency_failure_modes_review`, `api_surface_review`, `docs_drift_review` rev 2). `driver-cleanup-code-review.md` excluded (predates this series).
- **Method:** every Critical/High finding was enumerated from the documents and independently re-verified against the code at this commit, with the explicit goal of **refuting** each one. Verification used: (a) direct code reading at every cited file:line; (b) whole-repo greps for every dead-code/zero-caller claim (excluding `docs/reviews/`); (c) runtime reproduction against the mock driver / offline package — never hardware — for ZC-01, CF-01, CF-02, LT-02/LS-34, and LT-03; (d) caller/data-flow tracing for every claimed consequence. The factual premises of refactor proposals RF-01–RF-05 were re-verified the same way. `docs_drift_review` (rev 2, already execution-verified) was spot-checked on its four Highs rather than fully re-derived.
- **Runtime evidence produced during this verification:**
  - ZC-01: `mic1 = set_instrument(a); mic2 = set_instrument(b); mic1.get_xyz()` → `RuntimeError: session is disconnected` (mock); `layer.set_instrument` keeps both live.
  - CF-01: an API confirmation leg fired through `router._fire_api_read(leg, id(client))` — its inner routed `get_jobs(mode="api")` **never reached the client** (a fake client whose attribute access would have errored produced zero attribute errors), spun exactly 2.00 s on the held in-flight claim, logged `api read not started: another read in flight past 2.0s`, and returned `None`; the identical leg run outside the race reached the client in 0.00 s.
  - CF-02: `check_idle(dead_client, timeout=None)` did not return within an 8 s watchdog; with `timeout=0.1` it returned a structured `success=False` ("Pre-check timeout ... status: Unknown").
  - LT-02/LS-34: `test_missing_xml_raises` measured at **60.06 s** alone.
  - LT-03: `TestConfirmFunctions` measured at 28.22 s with nine tests at 3.00–3.01 s each (full `CONFIRM_POLL_S` window burned per negative confirm).

**Result: 30 of 30 Critical/High findings CONFIRMED (0 refuted, 0 downgraded, 0 unverifiable-offline), plus all five RF-01–RF-05 factual premises verified.** Two findings carry corrections to their supporting rationale (LS-01, LT-04 — details in §3); neither correction changes the verdict or severity.

---

## 1. Verdict table

Where several documents file the same defect (e.g. LA-01/LM-01/OP-02/DD-04), each ID gets its own row; shared evidence is cited once.

| ID | Doc | Severity | One-line claim | Verdict | Key evidence |
|---|---|---|---|---|---|
| ZC-01 | zmart_controller | High | Docstring's multi-microscope recipe disconnects the first microscope | **CONFIRMED** (executed) | `__init__.py:16-20` docstring vs `:43-58` wrapper; reproduced against mock — `mic1.get_xyz()` raises after second `set_instrument` |
| ZC-02 | zmart_controller | High | Flagship notebook crashes on stale `mutable`/`immutable` state keys | **CONFIRMED** | Cells 7–8 index `prescan["mutable"]`; mock/adapters/README all return `changeable`/`observed` (`mock_driver.py:219-220`); cell 11 references nonexistent `zmart.set_state`. Execution-verified by DD-02 (rev 2) |
| LC-01 | leica_commands | High | `capabilities.py` evidence-leg subsystem (~90 lines + `xy_min_delta_um`) is dead | **CONFIRMED** | Repo-wide grep: `change_spec`/`key_delta`/`xy_min_delta_um` appear only at their definitions (`capabilities.py:66,241`; `profiles.py:128`) |
| LC-02 | leica_commands | High | `select_job` log-evidence window opens before the unbounded idle pre-check; freshness backstop ships disabled | **CONFIRMED** | `commands.py:1438` captures `command_started_at` before `_dispatch`; `profiles.py:449-450` SELECT_JOB ships `check_idle(timeout=None)` (runs inside `_fire_block`, after the capture); `log_wait.py` admissibility is `ts > command_started_at`; the backstop chain `selected_job_log_cluster_max_age_s=None → _too_old(max_age_s=None) → LOG_READER.max_age_s=None → False` verified (`profiles.py:69,117`; `log_reader.py:394-401`) |
| LS-01 | leica_scanfields | High | `parse_lrp` (380-line load-bearing parser) has zero unit tests | **CONFIRMED — rationale corrected** | Grep of `tests/` + `calibration/tests/`: zero references to `parse_lrp`/`scanfields.lrp`. Load-bearing via `_get_job_names` (`parsers.py:966`, on the production `parse_scan_positions` path) and `save_and_read_lrp` (`files.py:266`). Correction: the cited consumers `lrp_verify_roi_count`/`lrp_verify_roi` are themselves uncalled (see §3) |
| LS-21 | leica_scanfields | High | `acquisition/ome.py` (incl. binary TIFF patcher) has no direct tests | **CONFIRMED** | Grep: every test reference to `check_ome_*`/`fix_ome_*`/`_read_tiff_tag_270` is a `patch(...)` (`test_acquisition.py:150-162,603,655-660,676-677`; `test_native_autosave.py:428`); the fixer is load-bearing in production (`materialize.py:94-98` repairs in place) |
| LS-34 | leica_scanfields | High | One test burns 60 s of real wall clock (≈88 % of the in-scope suite) | **CONFIRMED** (measured) | `test_missing_xml_raises` re-measured at **60.06 s**; it passes no `export_completion_timeout` and `DEFAULT_EXPORT_COMPLETION_TIMEOUT_S = 60.0` (`navigator_expert_export.py:37`) |
| LM-01 | leica_calibration | High | Calibrated backlash block validated/loaded, then largely ignored; `tolerance_um`/`approach` consumed by nothing | **CONFIRMED** | Adapter calls both primitives bare (`zmart_adapter.py:600,753,892`); repo-wide grep: `tolerance_um` appears only in schema validation/copy-through (`stage_config.py:46,141`, `model.py:110`) and tests; `approach` only in schema/tests/`calibration.json`; workflow passes only `overshoot_um`/`settle_ms` (`focus.py:330-331`, `_acquire.py:45-46`) |
| LM-02 | leica_calibration | High | `correct_backlash` accepts unconfirmed moves, contradicting its sibling's contract | **CONFIRMED** | `movement.py:96-104` (`move_xy_with_backlash`: `success` **and** `confirmed` on both legs, with rationale comment) vs `:146-151` (`correct_backlash`: `success` only); `success_on_unconfirmed=True` is the shipped default (`profiles.py:246`) |
| LA-01 | leica_zmart_adapter | High | Calibrated backlash silently ignored on all controller-path moves; coincidence with bundled values masks it | **CONFIRMED** | Same call sites as LM-01; bundled `calibration/defaults/calibration.json` backlash = 50/100/20, identical to the hardcoded fallbacks (`movement.py:41,110`) — the "silent today" claim verified exactly |
| LT-01 | leica_tests_ci | **Critical** | `controller.yml` runs `pytest controller/tests` — directory does not exist; workflow can never pass | **CONFIRMED** | `.github/workflows/controller.yml:36` reads `python -m pytest controller/tests --tb=short`; `ls controller` → no such directory; real path is `zmart_controller/tests`. pytest exits 4 on a missing path, so every trigger (push/PR to main) fails |
| LT-02 | leica_tests_ci | High | `test_missing_xml_raises` burns the full 60 s default export timeout | **CONFIRMED** (measured 60.06 s) | Same evidence as LS-34 |
| LT-03 | leica_tests_ci | High | ~35 s of real, unmocked poll-window sleeps across `test_core_driver.py` | **CONFIRMED** (measured) | `TestConfirmFunctions` alone: nine tests at 3.00–3.01 s (27 s of the class's 28.22 s); the 1 s echo-settle stalls in `TestConfirmAndFire`/`TestConfirmation` verified structurally (`dispatch.py:128` `timeout=1.0`, mock echo never settles post-flush) |
| LT-04 | leica_tests_ci | High | Riskiest byte-level code effectively untested; coverage measured but enforced nowhere | **CONFIRMED — partially re-measured** | Structural premise fully verified: zero tests exercise `scanfields/lrp.py` or `acquisition/ome.py` unmocked (see LS-01/LS-21); `.coveragerc` has no `fail_under`; `run_ci.py:219-222` silently skips coverage when pytest-cov is absent. The exact percentages (7 %/41 %/13–31 %) were not independently re-run; they are consistent with the verified zero-direct-test facts and immaterial to the verdict |
| FD-01 | fossils_dead_code | High | Evidence-leg subsystem dead (full inventory) — confirms LC-01 | **CONFIRMED** | Same greps as LC-01; the six `_selected_job_*`/`_xy_*` helpers referenced only by the dead `DatumSpec` fields |
| FD-02 | fossils_dead_code | High | `experimental/lrp_edits/`: 3 production-consumed functions in 2,192 lines; 24 dead `lrp_verify_*`; dead coordinate helpers | **CONFIRMED (and extended)** | `commands.py:1165-1166` imports exactly `galvo_pan_for_pixel`/`lrp_get_pan`/`lrp_set_pan`; repo-wide grep: only two `verify_fn=` call sites ever pass a verifier (`roi.py:188`, `scan.py:321`), both inside the uncalled `disable_roi_scan`/`reset_pan`; no test/workflow/notebook uses any of the 24; `pixels_to_roi`/`center_vertices` have zero external references. Extension: `lrp_verify_roi`/`lrp_verify_roi_count` are *also* dead (§5) |
| FD-03 | fossils_dead_code | High | `require_canonical_scan_orientation` is a safety validator that no code ever calls | **CONFIRMED** | Repo-wide grep: definition (`session.py:87`), facade re-export, README only. Also re-verified the LC-10 fail-open: `get_lasx_settings() or {}` → `orient.get("enable_transform", False)` → check passes when settings are unreadable (`session.py:107-112`) |
| FD-04 | fossils_dead_code | High | Six orphan one-shot scripts in `tests/hardware/`, 2,686 lines, referenced by nothing | **CONFIRMED** | Per-script repo-wide grep (all file types, `docs/reviews` excluded): zero references for all six; `wc -l` totals **exactly 2,686** |
| OP-01 | overfitting_patchwork | High | `strip_template` warns-and-returns-success when the strip demonstrably failed; in-place sibling correctly fails | **CONFIRMED** | `strip_restore.py:142-152`: residual-object count → `log.warning` → `return {"success": True, ...}`; `strip_template_in_place` (`:236-244`): identical condition → `log.error` → `return None`. Consumer `_ensure_scan_fields_stripped` (`zmart_adapter.py:684`) tests only truthiness, so a failed strip passes and `acquire` proceeds against a template that still carries scan fields |
| OP-02 | overfitting_patchwork | High | Backlash defaults shadow calibrated values (confirms LA-01/LM-01) | **CONFIRMED** | Same evidence as LA-01/LM-01 |
| OP-03 | overfitting_patchwork | High | select_job evidence window pre-fire + disabled freshness backstop (confirms LC-02) | **CONFIRMED** | Same evidence as LC-02; the OP-05 aliasing (`_selected_job_name_from_log` locally re-defaults `None` → 2.0 s) also sighted in `confirm_select_job.py` |
| CF-01 | concurrency | High | Hybrid confirmation race's API leg self-blocks on its own in-flight claim — never performs a CAM read | **CONFIRMED** (independently reproduced) | Code chain: `commands.py:216` passes `api_key=id(client)` iff a log leg exists → `race_confirmations` runs the leg inside `_fire_api_read` which claims that key (`router.py:95-105`) → the leg's poll body `_readers.get_jobs(client, mode="api")` routes to `_capped_api_read(..., api_key=id(client))` (`router.py:216`) → `_claim_api_read` is a non-reentrant set (`router.py:130-137`) → refused for the full 2 s timeout. Runtime repro (above): zero client accesses inside the race, instant success outside it |
| CF-02 | concurrency | High | `check_idle(timeout=None)` on every hardware profile → permanent hang when LAS X hangs/crashes pre-fire | **CONFIRMED** (independently reproduced) | `prechecks.py:50-76`: `while True`, `None`/`"Unknown"` = not idle, no exit when `timeout is None`; all five profiles ship it (`profiles.py:311,407,417,430,450`). Repro: no return within watchdog against a dead client |
| CF-03 | concurrency | High | `confirm_acquire` with `poll_timeout=None` loops forever once `saw_scanning` when LAS X dies mid-scan | **CONFIRMED** | `confirmations.py:1096` `deadline = t_start + 1e9`; `status None/"Unknown"` → `consecutive_idle = 0` (`:1106-1114`); the permanent-error/start-timeout block is guarded by `if not saw_scanning:` (`:1120`) — no exit path remains once phase 2 is armed; ACQUIRE ships `poll_timeout=None` (`profiles.py:442`) |
| AS-01 | api_surface | High | `confirmed` enforced at only one of six seam verification points; `set_state` reports an unconfirmed job selection as applied | **CONFIRMED** | Enforced: `zmart_adapter.py:748` (`move_z`, `success and confirmed`) + `move_xy_with_backlash` internally. Success-only: `acquire`'s select (`:749-750`), `set_state` (`:862-864`), both autofocus selects (`:924-926,934-936`), `capture.acquire` (`capture.py:57-58`), `correct_backlash` (`movement.py:146-151`). `success_on_unconfirmed=True` default verified (`profiles.py:246`) |
| DD-01 | docs_drift | High | Confirms ZC-01 (executed) | **CONFIRMED** | Same execution evidence as ZC-01 |
| DD-02 | docs_drift | High | Confirms ZC-02 (executed) | **CONFIRMED** | Same evidence as ZC-02 |
| DD-03 | docs_drift | High | Confirms FD-03 + fails-open detail + README instructs calling a gate no code path enforces | **CONFIRMED** | Same greps/code as FD-03; README:85-101/187/392 claims re-read and match |
| DD-04 | docs_drift | High | Confirms LM-01/LM-02: docstrings promise calibrated params the production path never passes | **CONFIRMED** | `movement.py:23-25` docstring text verified verbatim; call sites as LM-01 |
| RF-01 (premise) | refactor | rank 1 | `TestAcquisitionProtocol` tests its own mocks; fossil removed-API/hasattr tests; redundant bootstraps | **PREMISE VERIFIED** | `test_core_driver.py:2175-2270`: table + `_run_protocol` loop defined in the test file; `test_full_protocol_completes` patches `drv.select_job`, `drv.move_xy`, `drv.acquire`, `commands.confirm_and_fire`. `TestApiSetRemoved` (:1155), `TestModuleStructure` (:2089), `TestReadbackCacheRemoved` (:2721) all present. Minor nuance: the `set_*` wrappers do execute down to the patched `confirm_and_fire`, so "zero production code" is marginally overstated — but that wiring is already pinned by `TestSetFunctionWiring`, so the deletion rationale holds |
| RF-02 (premise) | refactor | rank 2 | ~100 s of the suite is literal sleeping; `CONFIRM_POLL_S` bound by value at import | **PREMISE VERIFIED** (measured) | 60.06 s (LT-02) + 27 s (LT-03 class) measured; `confirmations.py:46` `from ..utils import CONFIRM_POLL_S` (value-bound — patching `utils.CONFIRM_POLL_S` would not propagate, exactly as the proposal warns) |
| RF-03 (premise) | refactor | rank 3 | Dead evidence subsystem + hybrid passive race unreachable at shipped defaults | **PREMISE VERIFIED** | LC-01 greps; all six `*_mode` fields default `"api"` (`profiles.py:88-123`); repo-wide grep for production `mode="hybrid"`/`mode="log"` on routed reads: none outside tests/hardware CLI |
| RF-04 (premise) | refactor | rank 4 | Four flush-fire-poll copies in `api_reader.py`; `jobName` correlation only in `get_job_settings` | **PREMISE VERIFIED** | Four skeletons at `api_reader.py:88,162,197,240`; `jobName` correlation exists only at `:127-132` |
| RF-05 (premise) | refactor | rank 5 | `experimental/lrp_edits` has exactly three production-consumed functions; `pixels_to_roi`/`center_vertices` dead | **PREMISE VERIFIED** | `commands.py:1165-1166` (function-local imports); dead-pair grep clean |

---

## 2. Non-CONFIRMED verdicts — evidence trails

None. No Critical/High finding was refuted or downgraded. Two rationale-level corrections that do **not** change any verdict are documented in §3 (LS-01, LT-04), and the deliberate severity-rubric note below applies to three findings.

**Severity-rubric note (no downgrades issued).** LC-01/FD-01 (dead subsystem), FD-04 (orphan scripts), and LS-34/LT-02/LT-03 (test speed) are High under their documents' explicitly stated rubrics ("significant dead weight" / maintainer-cost math: 60 s × every run × 9 CI jobs). On a pure production-risk scale they would rank Medium; the consolidated report should present them under a "maintenance/dead-weight" heading rather than alongside wrong-hardware-action Highs like OP-01/CF-01/AS-01, but reclassifying them would misrepresent the reviews' own severity definitions, which are internally consistent.

---

## 3. Corrections and adjudications

Rationale corrections (verdicts unchanged):

1. **LS-01's load-bearing justification is partly wrong.** LS-01 states `parse_lrp` "is load-bearing for verification (`roi.py:646,675` — `lrp_verify_roi_count`/`lrp_verify_roi` gate `apply_lrp_change` success)". Verified: **those two verifiers have zero callers anywhere in the repo** (only their definitions and facade re-exports). `parse_lrp`/`lrp.py` is nonetheless genuinely load-bearing — `_get_job_names` is called on the production `parse_scan_positions` path (`parsers.py:966`, reached from the adapter's `get_context`/`_scan_field`), and `save_and_read_lrp` (`files.py:266`) consumes `parse_lrp` — so the finding (zero tests for a load-bearing 380-line parser) stands with the corrected consumer list.
2. **LT-04's coverage percentages were not independently re-measured.** The verifiable core — no test exercises `lrp.py` or `ome.py` unmocked, no `fail_under` in `.coveragerc`, coverage silently optional in `run_ci.py` — was fully re-verified. The quoted 7 %/41 %/13–31 % figures come from the LT review's own instrumented run and are consistent with the verified facts; treat them as measured-once, not twice.

Cross-document adjudications:

3. **FD-07 vs LM-19 on `LIMITS_SOURCE_CFG_FALLBACK` — FD-07 is right.** LM-19 called `cfg_fallback` test-only; verified: `workflows/target_acquisition/pipeline/template.py:142` selects `drv.LIMITS_SOURCE_CFG_FALLBACK` in production. Keep the constant; LM-19's deletion advice applies only to `LIMITS_SOURCE_MIGRATION` (which grep confirms is written and read by nothing).
4. **LA-05's "136 of 216" vs FD-05's "135 of 216" — immaterial counting-method difference.** The total of 216 `__all__` names was independently re-verified by AST (216 exactly; 14 underscore-private names + `__version__`, matching LA-06's list byte-for-byte). The one-name delta does not affect any action; the consolidated report should quote "≈135".
5. **`ome.py` / `ome_canonical.py` unification — the refactor review's rejection (R-02) is upheld, and there is no genuine conflict.** No earlier review actually proposed a full merge: LS-32 asked only for seam fixes (fix a stale docstring, promote `_read_tiff_tag_270`, consider one converters module), which RF-12 adopts. Verified: the only cross-import between the two modules is `ome_canonical.py:208` reaching `_ome._read_tiff_tag_270` — one shared helper between a byte-preserving patcher and an ElementTree generator. R-02's factual basis is correct.
6. **LC-11/FD-11 (passive hybrid read race unreachable) vs CF-01 (hybrid confirmation race API leg dead) — complementary, both verified.** They concern two different pieces of hybrid machinery with different root causes (shipped `"api"` defaults vs non-reentrant claim design). Together they mean *no hybrid mechanism in the driver is actually operative at runtime* — worth stating once in the consolidated report.
7. **DD-09's rev-2 self-correction verified.** `initial_positions` does exist in the mesoSPIM adapter (`mesospim_zmart_adapter.py:91,169`), so rev 2's corrected phrasing ("mock/mesoSPIM-only key", not "mock-only") is the accurate one.
8. **CF-05's budget arithmetic verified** (supporting the cluster in §4): `confirm_select_job.py:207-212` sets `budget_s = min(6.0, effective_timeout)` = 5.0 with SELECT_JOB's `poll_timeout=5.0`, while the API leg's own poll window is the same 5.0 s — the abandoned leg systematically outlives the race, as claimed.

---

## 4. Root-cause cluster map

Findings reported by multiple agents that share a single root cause. Members in **bold** are Critical/High.

| # | Canonical name | Members | Root cause (one line) |
|---|---|---|---|
| 1 | **ignored-calibrated-backlash** | **LA-01, LM-01, OP-02, DD-04**, FD-07, OP-18, FD-18/LM-20 | The adapter (and partially the workflow) never threads `stage_cfg["backlash"]` into the motion primitives, so hardcoded 50/100/20 shadows the machine snapshot and `tolerance_um`/`approach` are schema ornaments |
| 2 | **accepted-vs-confirmed seam** | **AS-01, LM-02**, (DD-04 second half), LA-14, LT/LM-24 | `success_on_unconfirmed=True` is the shipped default and five of six seam verification points test only `.get("success")`, so accepted-but-unverified commands are reported as applied |
| 3 | **paper-safety-gates** | **FD-03, DD-03**, LC-10, OP-24/LS-09 | Safety checks that exist only as prose or fail open when unevaluable: the orientation gate has zero callers and passes on unreadable settings; template state "fresh" doubles as "environment unknowable" |
| 4 | **select-job-evidence-window** | **LC-02, OP-03**, CF-04, OP-05, CF-12/DD-24 | The log-evidence admissibility window is anchored at wrapper entry (wall clock), before an unbounded idle pre-check, with the freshness backstop disabled by a `None` default that a second consumer silently re-interprets as 2.0 s |
| 5 | **fail-hung-unbounded-waits** | **CF-02, CF-03**, OP-16 | Fail-closed reads ("Unknown" = not idle / not done) composed with `timeout=None` profiles turn a dead LAS X into a permanent driver hang instead of an error |
| 6 | **dead-evidence-leg-subsystem** | **LC-01, FD-01, OP-21, RF-03(a)** | A speculative change/target-evidence race shipped in `capabilities.py` with no consumer; its docstring claims it powers the confirmation race, which never touches it |
| 7 | **inoperative-hybrid-machinery** | **CF-01**, CF-05, LC-11, FD-11, RF-03(b) | No hybrid path actually runs: the confirmation race's API leg self-blocks on its own non-reentrant in-flight claim (and its abandoned leg outlives the budget), while the passive hybrid read race is unreachable because every profile mode defaults `"api"` |
| 8 | **uninjected-test-sleeps** | **LS-34, LT-02, LT-03**, LC-13, RF-02 | Confirm windows, echo-settle timeouts, and export-completion timeouts are not injected (or injected inconsistently), so ~95–100 s of the ~124 s offline suite is literal sleeping |
| 9 | **untested-byte-level-code** | **LS-01, LS-21, LT-04**, LS-35 | The highest-risk parsers/patchers (LRP parser, binary TIFF tag-270 surgery, strip/restore failure ladder) are exercised only via mocks or not at all, and coverage is collected but enforced nowhere |
| 10 | **dead-lrp_edits-surface** | **FD-02, RF-05**, LS-13, LS-14, LS-15, LC-23 | `experimental/lrp_edits` is simultaneously load-bearing (3 functions feed `move_galvo_to_pixel`) and mostly dead (24 verify wrappers + coordinate helpers + ~60 consumer-less exports), under a package name that misleads in both directions |
| 11 | **orphan-hardware-scripts** | **FD-04**, LT-08 | Six finished one-shot debugging/measurement scripts (2,686 lines) remain in `tests/hardware/` referenced by nothing |
| 12 | **teardown-gap** | **ZC-01, DD-01**, CF-06, ZC-03/RF-15 | Instrument swap semantics: the module-level `set_instrument` tears down the previous session (contradicting its own docstring), while the driver-side `disconnect` is flag-only — no CAM client teardown exists, so reconnect cycles leak connections |
| 13 | **fossil-self-referential-tests** | LT-06, LT-09, FD-14, RF-01 | Removed-API tombstones, hasattr inventories, a version pin, and a 360-line protocol suite that patches out all production code and asserts on its own loop |
| 14 | **flush-fire-poll-drift** | LC-09, OP-14, RF-04, OP-04 (hazard link), OP-31 | The CAM read skeleton was copy-pasted four times and the stale-response correlation guard was added to only one copy, which also makes the `observed_after` freshness gates ineffective for A→B→A patterns |
| 15 | **broken-tuning-advice** | LA-07, DD-06, OP-26, LC-18 | `utils.py` documents "import and override" for `RECEIPT_TIMEOUT`/`CONFIRM_POLL_S`, but every consumer binds the values at import time; transport/echo tuning also lives outside the profile system built for exactly this |
| 16 | **packaging-gap-bootstrap-constellation** | ZC-14, LA-19, LT-10, LM-26, LM-30, OP-33, RF-17 | No `[project]` table in `pyproject.toml` → ≥20 `sys.path` bootstrap copies across conftest files, notebooks, per-test-file inserts, and `_bootstrap.py` shims |
| 17 | **facade-bloat** | LA-05, LA-06, FD-05, FD-20, FD-24, RF-06, AS-20, DD-21 | The 216-name driver facade exports ≈135 names nothing consumes via the root, including 14 underscore-private names and the mutable `_stage_limits` safety dict |
| 18 | **marker-fiction** | LT-05, FD-15, DD-07 | `hardware`/`slow` pytest markers are registered, documented in three places, and filtered on — and used by zero tests; the real offline/online split is filename-based |

---

## 5. Critical/High issues missed by all eleven documents

**None found at Critical or High severity.** The review series is unusually well cross-verified (the fossils, overfitting, concurrency, and docs passes each re-checked earlier claims at this commit, and this verification refuted none of them).

One below-threshold addendum discovered during verification, recorded so the consolidated report can fold it into FD-02's deletion worksheet:

- **`lrp_verify_roi` and `lrp_verify_roi_count` (`experimental/lrp_edits/roi.py:635-703`, ~70 lines) are also dead** — their only external references are the facade re-export (`__init__.py:190-191,492-493`). FD-02's 24-wrapper dead list (focus/general/scan/z) did not include them, and LS-01 mistakenly cited them as the load-bearing consumers of `parse_lrp`. Severity: Medium (dead code + a stale justification in a prior review), same action as FD-02 group (a): delete.
