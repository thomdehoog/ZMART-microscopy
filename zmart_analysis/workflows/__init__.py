"""Shared helpers and the analysis workflows.

The steps themselves are loaded by path by the engine and put this directory
on ``sys.path``; this package marker is so ZMART can import the shared
helpers by name, as ``zmart_analysis.workflows._geometry``.
"""
