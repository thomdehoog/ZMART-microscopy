# Minimum runtime environment for ZMART Microscopy -- pip fallback.
#
# The CANONICAL install is the conda-forge environment in environment.yml
# (build it with build_env.py). This pip list mirrors the same packages for
# environments without conda. Note: conda's `opencv` is `opencv-python` on
# PyPI; otherwise the names match.
#
# Install into a Python 3.10-3.12 env:
#     python -m pip install -r requirements.txt
#
# Test/lint/browser tools are NOT here -- install root requirements-dev.txt for
# the complete pip-based CI environment.

# --- compute core (registration, focusing, image I/O, calibration) ---
numpy>=1.24
scipy>=1.10
scikit-image>=0.22,<0.26 # registration: ORB, RANSAC, phase_cross_correlation
opencv-python>=4.8      # registration: cv2 feature matching
tifffile>=2024.1        # read/write OME-TIFF image data
pillow>=10              # PNG/image encoding used by reports and widgets
ome-types>=0.5          # OME-XML model: validates the driver's OME-TIFF output
lxml>=4.9               # ome-types schema-validation backend (validate=True)

# --- live LAS X API (Python <-> .NET bridge) ---
# The LAS X CAM API DLLs ship with LAS X and load from the install dir;
# only the bridge itself is pip-installable.
pythonnet>=3.0

# Operator notebook and interactive target-acquisition widgets.
matplotlib>=3.7
ipympl>=0.9
# React-based notebook widgets (zmart_microscopy_v4_react.ipynb).
anywidget>=0.9
traitlets>=5.9
