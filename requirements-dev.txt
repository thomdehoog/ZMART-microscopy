# The tools for running the test suite, on top of requirements.txt.
#
#     python -m pip install -e ".[dev]"
#     python -m playwright install chromium

-r requirements.txt

pytest>=8
ruff>=0.6

# Drives a real browser. About a third of this suite opens one, draws the data and
# looks at the pixels that came out, because the fault this viewer keeps meeting is
# a picture that is silently absent: pieces fetched, layers built, the engine
# reporting itself content, and nothing on screen. Only looking catches that.
playwright>=1.58,<2

# Opens the screenshots those tests take, which is how the pixels get counted.
pillow>=10
