# The tools for running the test suite, on top of requirements.txt.
#
#     python -m pip install -e ".[dev]"
#     python -m playwright install chromium

-r requirements.txt

pytest>=8
ruff>=0.6

# Drives a real browser. About a third of this suite opens one, draws the data and
# looks at the pixels that came out, because the fault this viewer keeps meeting is
# a picture that is silently absent: pieces fetched, layers built, the engine
# reporting itself content, and nothing on screen. Only looking catches that.
playwright>=1.58,<2

# Opens the screenshots those tests take, which is how the pixels get counted.
pillow>=10

# Somebody else's OME-Zarr reader, used to check that the images this project writes
# can be opened by other people's software and land in the right place. Our own
# viewer reading our own images cannot catch a misunderstanding that both halves
# share, and one such misunderstanding did reach real data — see viz_studio/INTEROP.md.
# This is the reader that multiview-stitcher and much of the Python imaging world
# use, so it stands in for them. Nothing in ZMART needs it in order to run.
ngff-zarr[validate]>=0.41
