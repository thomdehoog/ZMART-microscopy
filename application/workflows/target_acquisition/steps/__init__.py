"""One folder per step, in both languages.

The reading order is not in these names -- it is written out in `the-run.js`
and `the_run.py`, which compose them. Numbered folders said it twice and cost
more than they were worth: no Python module can live in a folder called
`4_focus_strategy`, so a step's two editions could not share a home.
"""
