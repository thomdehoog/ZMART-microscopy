"""Raw LAS X command/readback mechanics.

Contains the core modules that implement the command dispatch
pipeline: utils, errors, settings, prechecks, confirmations, dispatch,
profiles, commands, objectives, and session. State readout lives in the
sibling ``state_readers`` package.
"""
