# Dev / test dependencies for the mesoSPIM ZMART driver.
#
# The MIT client itself (connection/, commands/, motion/, readers/,
# mesospim_zmart_adapter.py) has NO heavy dependencies -- it is pure
# standard-library sockets + JSON. Everything below is only for the test suite.
#
#   pip install -r zmart_drivers/mesospim/requirements-dev.txt

pytest>=7          # test runner
numpy>=1.21        # synthetic frames in the mock server / tests
tifffile>=2021.7   # read/write those frames in the mock server / tests
