# Generated test/CI artifacts
tests/_report/
# Python caches
__pycache__/
*.pyc
# Local connection config (never commit tokens/certs)
config.ini
# What `python -m zenapi.simulator` writes by default (certificate, token, fake images)
fake_zen_gateway/
