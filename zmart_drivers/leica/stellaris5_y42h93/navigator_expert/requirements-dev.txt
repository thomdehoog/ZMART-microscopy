# Test/CI dependencies for the Navigator Expert driver offline suite.
#
# One source of truth for what the suite needs, installed both by CI
# (.github/workflows/navigator-expert.yml) and locally:
#     python -m pip install -r requirements-dev.txt
#     python run_ci.py
#
# Every heavy dependency below is a *traced* transitive import of the offline
# suite, not a guess -- the comment says which code reaches it. NOTE: pythonnet /
# the LAS X CAM runtime are deliberately absent; they are only needed for the
# live @hardware tests on a microscope PC. The offline suite runs anywhere
# (Linux/macOS CI included).

# Tooling.
pytest>=8
pytest-cov>=5
ruff>=0.6

# Core numerics + imaging IO (driver acquisition, scanfield parsing, save()).
numpy>=1.24
scipy>=1.10
tifffile>=2024.1
pillow>=10
ome-types>=0.5           # OME-XML model in tests/unit/test_acquisition.py
lxml>=4.9                # ome-types XML-validation backend (else: "requires lxml or xmlschema")

# Calibration subsystem (lives inside the driver) + shared.algorithms.
matplotlib>=3.7          # calibration report figures (calibration/tests)
scikit-image>=0.21       # shared.algorithms.registration: ORB/RANSAC/phase-corr
opencv-python-headless>=4.8  # cv2 in shared.algorithms.registration; headless for CI
ipython>=8               # calibration visualize: `from IPython.display import display`
anywidget>=0.9           # React notebook widgets (workflows/target_acquisition tests)
ipympl>=0.9              # interactive matplotlib canvas the v4 notebook cells import
nbformat>=5.9            # read the v4 notebooks (guard + end-to-end tests skip without it)
nbclient>=0.9            # execute the v4 notebooks offline in the end-to-end tests
ipykernel                # kernel the notebook end-to-end execution runs in
