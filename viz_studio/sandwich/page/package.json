{
  "name": "zmart-sandwich-probe",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "node precompile-workers.mjs && vite build && node copy-async-worker.mjs"
  },
  "dependencies": {
    "@deck.gl/core": "^9.1.0",
    "@deck.gl/layers": "^9.1.0",
    "neuroglancer": "2.41.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "vite": "^7.0.0"
  }
}
